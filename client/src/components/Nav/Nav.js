import React from "react";

const Nav = () => (
  <nav style={{zIndex: 500, backgroundColor: "black", position: "fixed"}} className="navbar navbar-expand-lg">
    <a style={{fontFamily: 'one_pieceregular'}} className="navbar-brand" href="/" title="Crew Select">
      Select a Crew
    </a>
  </nav>
);

export default Nav;
